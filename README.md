# AI Driven Development Template (v2.1)

## Prerequisites (前提条件)
This template requires the **Linear MCP Server** to be configured in your Claude setup.
Skills like `suspend`, `resume`, `ticket-feedback`, and `handle-ci-failure` rely on Linear API.
